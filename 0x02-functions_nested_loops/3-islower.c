#include "main.h"
/**
 * _islower - Function
 *
 * Description: checks if a character os lower case
 * @c: character to check the case
 * Return: 0
 */
int _islower(int c)
{
	return (c >= 97 && c <= 122);
}
